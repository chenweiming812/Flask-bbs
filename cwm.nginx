server {
    listen 80;

    location /images {
        alias /var/www/cwm/images;
    }

    location /static {
        alias /var/www/cwm/static;
    }

    location / {
        proxy_pass http://localhost:4000;
    }
}

